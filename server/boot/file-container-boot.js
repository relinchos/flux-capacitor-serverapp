module.exports = function FileStorageBoot(app) {

	var FileStorage = app.models.FileStorage;

	// FileStorage.createContainer({ name: 'avatars'}, function(err, container){
	// 	if(err){
	// 		if(err.code === 'EEXIST'){
	// 			console.log('File container "avatars" hasn\'t been created because it already exist.')
	// 		}else{
	// 			console.error('Error creating file container: ',err);
	// 		};
	// 		return;
	// 	};
	// });

};

